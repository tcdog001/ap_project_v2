#!/bin/bash

if [[ -z "${__UTILS_TIME_IN__}" ]]; then __UTILS_TIME_IN__=_; else return; fi

getdate() {
	date '+%F'
}

gettime() {
	date '+%H:%M:%S'
}

getnow() {
	date '+%F-%H:%M:%S'
}

__getnow() {
	date '+%F %H:%M:%S'
}
